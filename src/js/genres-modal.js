// import { genres } from '../data/genres.js';

// export function genresModalCard(array) {
//   const genreMovie = array.map(genre => genre.name);

//   if (!genreMovie.length) {
//     return 'Unknown genre';
//   } else if (genreMovie.length > 3) {
//     return `${genreMovie[0]}, ${genreMovie[1]}, ${genreMovie[2]}, Other`;
//   } else {
//     return genreMovie.join(', ');
//   }
// }